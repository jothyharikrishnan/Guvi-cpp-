#include <iostream>
#include<cmath>
using namespace std;

int main()
{
    float a,res;
    int i;
    cout<<"Enter a decimal(only 1 decimal place) number:";
    cin>>a;
    res=round(a);
    cout<<res;
    return 0;
}
